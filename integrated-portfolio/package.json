{
  "name": "integrated-portfolio",
  "version": "1.0.0",
  "description": "Modern portfolio website with Vue.js frontend and Apollo GraphQL backend",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "start": "node dist/server/index.js",
    "build": "rm -rf dist && cd client && npm install && npm run build && cd .. && mkdir -p dist/client && cp -r client/dist/* dist/client/ && mkdir -p dist/server && cp -r server/* dist/server/ && cp server/.env.example dist/server/"
  },
  "keywords": [
    "portfolio",
    "vue",
    "graphql",
    "apollo",
    "postgresql"
  ],
  "author": "Creations Studio",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "express": "^5.1.0",
    "nodemailer": "^7.0.6"
  }
}
