{
  "name": "integrated-portfolio",
  "version": "1.0.0",
  "description": "Modern portfolio website with Vue.js frontend and Apollo GraphQL backend",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "start": "node public/server/index.js",
    "build": "rm -rf public && cd client && npm install && npm run build && cd .. && mkdir -p public/client && cp -r client/dist/* public/client/ && mkdir -p public/server && cp -r server/* public/server/ && cp server/.env.example public/server/"
  },
  "keywords": [
    "portfolio",
    "vue",
    "graphql",
    "apollo",
    "postgresql"
  ],
  "author": "Creations Studio",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "express": "^5.1.0",
    "nodemailer": "^7.0.6"
  }
}
